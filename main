
import 'package:chazspace/login_set.dart';
import 'package:flutter/material.dart';

void main() => runApp(chazspot());

class chazspot extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: LoginSet(),
    );


  }
}
